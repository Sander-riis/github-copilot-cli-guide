---
phase: 02-cli-intro-agents-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [index.html]
autonomous: true
requirements: [CLI-01, CLI-02, CLI-03, CLI-04, CLI-05, CLI-06, CLI-07]

must_haves:

  truths:
    - "CLI Intro tab shows copilot, copilot -p, copilot -sp, copilot --agent command patterns"
    - "Mode cycling (Shift+Tab: standard → plan → autopilot) is explained in a collapsible with a table"
    - "Keyboard shortcuts table documents @FILENAME, !COMMAND, Esc, Ctrl+L, /help"
    - "Power flags table documents --allow-all-tools, --autopilot, --agent, --resume, --allow-tool, --deny-tool"
    - "All 10 slash commands are listed in a reference table"
    - "/research special status (never auto-triggered) is called out in a callout--info box"
    - "No placeholder text 'Content for this section will be added in Phase 2' remains in cli-overview or cli-reference"
  artifacts:
    - path: "index.html"
      provides: "cli-overview and cli-reference section content"
      contains: "copilot -p"
  key_links:
    - from: "#cli-overview"
      to: "command patterns, mode cycling, shortcuts, power flags"
      via: "narrative paragraphs + collapsibles"
      pattern: "Shift\\+Tab"
    - from: "#cli-reference"
      to: "slash commands table + /research callout"
      via: "collapsible + callout--info"
      pattern: "/research"
---

<objective>
Replace all placeholder content in the CLI Intro tab's two sections (`#cli-overview` and `#cli-reference`) with complete, accurate HTML content covering all seven CLI requirements.

Purpose: The CLI Intro tab is the first thing readers see. It must deliver a full, accurate reference to how copilot CLI works — commands, modes, shortcuts, power flags, and slash commands — with narrative context, not just a list.

Output: `index.html` with `#cli-overview` and `#cli-reference` sections fully populated (no placeholder text).
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-cli-intro-agents-content/02-CONTEXT.md
@.planning/phases/02-cli-intro-agents-content/02-RESEARCH.md
@.planning/phases/01-technical-foundation/01-UI-SPEC.md
@index.html

<interfaces>
<!-- Copy-button HTML pattern — use EXACTLY this structure in every code-block__copy button.
     The dual-SVG pattern (icon--copy + icon--check icon--hidden) is required for the JS toggle to work. -->

COPY BUTTON (paste inside every .code-block__header):
```html
<button class="code-block__copy" aria-label="Copy to clipboard">
  <svg class="icon icon--copy" aria-hidden="true" width="14" height="14" viewBox="0 0 16 16" fill="currentColor">
    <path d="M0 6.75C0 5.784.784 5 1.75 5h1.5a.75.75 0 010 1.5h-1.5a.25.25 0 00-.25.25v7.5c0 .138.112.25.25.25h7.5a.25.25 0 00.25-.25v-1.5a.75.75 0 011.5 0v1.5A1.75 1.75 0 019.25 16h-7.5A1.75 1.75 0 010 14.25v-7.5z"/><path d="M5 1.75C5 .784 5.784 0 6.75 0h7.5C15.216 0 16 .784 16 1.75v7.5A1.75 1.75 0 0114.25 11h-7.5A1.75 1.75 0 015 9.25v-7.5zm1.75-.25a.25.25 0 00-.25.25v7.5c0 .138.112.25.25.25h7.5a.25.25 0 00.25-.25v-7.5a.25.25 0 00-.25-.25h-7.5z"/>
  </svg>
  <svg class="icon icon--check icon--hidden" aria-hidden="true" width="14" height="14" viewBox="0 0 16 16" fill="currentColor">
    <path d="M13.78 4.22a.75.75 0 010 1.06l-7.25 7.25a.75.75 0 01-1.06 0L2.22 9.28a.75.75 0 011.06-1.06L6 10.94l6.72-6.72a.75.75 0 011.06 0z"/>
  </svg>
  <span class="code-block__copy-label">Copy code</span>
</button>
```

COLLAPSIBLE CHEVRON SVG (paste inside every .collapsible__trigger):
```html
<svg class="collapsible__chevron" aria-hidden="true" width="16" height="16" viewBox="0 0 16 16" fill="currentColor">
  <path d="M6.22 3.22a.75.75 0 011.06 0l4.25 4.25a.75.75 0 010 1.06l-4.25 4.25a.75.75 0 01-1.06-1.06L9.94 8 6.22 4.28a.75.75 0 010-1.06z"/>
</svg>
```

SECTION BEING REPLACED — current placeholder in index.html (lines ~504–555):
```html
<section id="cli-overview" class="content-section">
  <h2 class="section-heading">CLI Intro — Overview</h2>
  <p>Content for this section will be added in Phase 2.</p>
  [... placeholder code blocks, collapsible, and callouts ...]
</section>
```

SECTION BEING REPLACED — current placeholder in index.html (lines ~557–579):
```html
<section id="cli-reference" class="content-section">
  <h2 class="section-heading">CLI Intro — Reference</h2>
  <p>Content for this section will be added in Phase 2.</p>
  [... placeholder yaml code block ...]
</section>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace #cli-overview with full CLI commands, modes, shortcuts, and power flags content</name>
  <files>index.html</files>

  <read_first>
    - index.html — read the entire `<section id="cli-overview">` block (lines ~504–555) to see exactly what to replace
    - .planning/phases/02-cli-intro-agents-content/02-RESEARCH.md — §CLI Commands, §Mode Cycling, §Keyboard Shortcuts, §Power Flags for verified technical content
    - .planning/phases/02-cli-intro-agents-content/02-CONTEXT.md — Decisions section for content style constraints
  </read_first>

  <action>
Replace the ENTIRE `<section id="cli-overview" class="content-section">` block (from the opening `<section` tag through its closing `</section>`) with the following HTML. Do not modify anything outside this section. Use the copy-button and chevron SVG patterns from the interfaces context exactly.

```html
        <section id="cli-overview" class="content-section">
          <h2 class="section-heading">CLI Overview</h2>
          <p>GitHub Copilot CLI is a standalone terminal AI agent — not a wrapper around <code>gh</code> and not the same as the IDE extension. You invoke it with <code>copilot</code> from any shell, and it maintains its own session context, tool permissions, and execution mode stack. Unlike the IDE extension, the CLI can traverse your filesystem, run shell commands, create and modify files, and interact with GitHub — without switching context from your terminal.</p>
          <p>The CLI is built for developers who work in the terminal. Run it interactively for open-ended exploration, send focused one-shot prompts with <code>-p</code>, or wire it into scripts. The same agents and skills ecosystem available in Copilot cloud works here too — your custom <code>.agent.md</code> files and <code>.github/skills/</code> directory are picked up automatically.</p>

          <h3 style="margin: 24px 0 8px;">Command Patterns</h3>
          <p>Four core invocation forms cover the majority of use cases. The bare <code>copilot</code> command opens a persistent interactive session; <code>-p</code> sends a single prompt and exits.</p>

          <div class="code-block" data-lang="bash">
            <div class="code-block__header">
              <span class="code-block__lang">bash</span>
              <button class="code-block__copy" aria-label="Copy to clipboard">
                <svg class="icon icon--copy" aria-hidden="true" width="14" height="14" viewBox="0 0 16 16" fill="currentColor">
                  <path d="M0 6.75C0 5.784.784 5 1.75 5h1.5a.75.75 0 010 1.5h-1.5a.25.25 0 00-.25.25v7.5c0 .138.112.25.25.25h7.5a.25.25 0 00.25-.25v-1.5a.75.75 0 011.5 0v1.5A1.75 1.75 0 019.25 16h-7.5A1.75 1.75 0 010 14.25v-7.5z"/><path d="M5 1.75C5 .784 5.784 0 6.75 0h7.5C15.216 0 16 .784 16 1.75v7.5A1.75 1.75 0 0114.25 11h-7.5A1.75 1.75 0 015 9.25v-7.5zm1.75-.25a.25.25 0 00-.25.25v7.5c0 .138.112.25.25.25h7.5a.25.25 0 00.25-.25v-7.5a.25.25 0 00-.25-.25h-7.5z"/>
                </svg>
                <svg class="icon icon--check icon--hidden" aria-hidden="true" width="14" height="14" viewBox="0 0 16 16" fill="currentColor">
                  <path d="M13.78 4.22a.75.75 0 010 1.06l-7.25 7.25a.75.75 0 01-1.06 0L2.22 9.28a.75.75 0 011.06-1.06L6 10.94l6.72-6.72a.75.75 0 011.06 0z"/>
                </svg>
                <span class="code-block__copy-label">Copy code</span>
              </button>
            </div>
            <pre class="code-block__pre"><code class="language-bash"># Interactive session — opens a persistent context window
copilot

# Non-interactive with a prompt — responds and exits
copilot -p "refactor src/auth/login.ts to use async/await"

# Short alias for -p
copilot -sp "what does the payment module do?"

# Launch with a specific custom agent active from the start
copilot --agent gsd-executor "run the phase 2 plan"</code></pre>
          </div>

          <details class="collapsible">
            <summary class="collapsible__trigger">
              <svg class="collapsible__chevron" aria-hidden="true" width="16" height="16" viewBox="0 0 16 16" fill="currentColor">
                <path d="M6.22 3.22a.75.75 0 011.06 0l4.25 4.25a.75.75 0 010 1.06l-4.25 4.25a.75.75 0 01-1.06-1.06L9.94 8 6.22 4.28a.75.75 0 010-1.06z"/>
              </svg>
              Mode Cycling — Standard, Plan, Autopilot
            </summary>
            <div class="collapsible__body">
              <p>During any interactive session, press <kbd>Shift+Tab</kbd> to cycle through three execution modes. The active mode is shown in the session prompt. You can also set the starting mode from the command line before the session opens.</p>
              <table>
                <thead>
                  <tr><th>Mode</th><th>Behavior</th><th>Best For</th></tr>
                </thead>
                <tbody>
                  <tr>
                    <td><strong>Standard</strong></td>
                    <td>Normal interactive — asks for confirmation before each tool use</td>
                    <td>Exploratory tasks, anything touching production code or external systems</td>
                  </tr>
                  <tr>
                    <td><strong>Plan</strong></td>
                    <td>Drafts a written implementation plan first; you review and approve before any execution begins</td>
                    <td>Complex multi-file changes where you want to audit the approach before committing</td>
                  </tr>
                  <tr>
                    <td><strong>Autopilot</strong></td>
                    <td>Auto-confirms all tool requests — fully autonomous execution</td>
                    <td>Trusted scoped tasks; CI-adjacent workflows; combined with <code>/fleet</code></td>
                  </tr>
                </tbody>
              </table>
              <div class="callout callout--tip">
                <span class="callout__type">Tip</span>
                <p>Start a session directly in autopilot mode with the <code>--autopilot</code> flag: <code>copilot --autopilot -p "run the full test suite and fix any failures"</code></p>
              </div>
            </div>
          </details>

          <details class="collapsible">
            <summary class="collapsible__trigger">
              <svg class="collapsible__chevron" aria-hidden="true" width="16" height="16" viewBox="0 0 16 16" fill="currentColor">
                <path d="M6.22 3.22a.75.75 0 011.06 0l4.25 4.25a.75.75 0 010 1.06l-4.25 4.25a.75.75 0 01-1.06-1.06L9.94 8 6.22 4.28a.75.75 0 010-1.06z"/>
              </svg>
              Keyboard Shortcuts &amp; Context Operators
            </summary>
            <div class="collapsible__body">
              <p>These shortcuts and prompt operators work inside any interactive Copilot CLI session:</p>
              <table>
                <thead>
                  <tr><th>Shortcut / Operator</th><th>Effect</th></tr>
                </thead>
                <tbody>
                  <tr><td><code>Shift+Tab</code></td><td>Cycle through Standard → Plan → Autopilot modes</td></tr>
                  <tr><td><code>@FILENAME</code></td><td>Include a file's content in the current prompt context — e.g. <code>@src/auth/login.ts</code></td></tr>
                  <tr><td><code>!COMMAND</code></td><td>Pass a shell command through without Copilot interpreting it — e.g. <code>!git status</code></td></tr>
                  <tr><td><code>Esc</code></td><td>Cancel the current operation</td></tr>
                  <tr><td><code>Ctrl+L</code></td><td>Clear the terminal screen — session context is preserved</td></tr>
                  <tr><td><code>/help</code></td><td>List all available slash commands for the current session</td></tr>
                </tbody>
              </table>
            </div>
          </details>

          <details class="collapsible">
            <summary class="collapsible__trigger">
              <svg class="collapsible__chevron" aria-hidden="true" width="16" height="16" viewBox="0 0 16 16" fill="currentColor">
                <path d="M6.22 3.22a.75.75 0 011.06 0l4.25 4.25a.75.75 0 010 1.06l-4.25 4.25a.75.75 0 01-1.06-1.06L9.94 8 6.22 4.28a.75.75 0 010-1.06z"/>
              </svg>
              Power Flags
            </summary>
            <div class="collapsible__body">
              <p>These flags modify session behavior at startup, before the session opens. They can be combined — <code>--autopilot</code> and <code>--allow-all-tools</code> together produce a fully non-interactive session suitable for scripting.</p>
              <table>
                <thead>
                  <tr><th>Flag</th><th>Effect</th></tr>
                </thead>
                <tbody>
                  <tr><td><code>--allow-all-tools</code></td><td>Skip all tool confirmation prompts for the session</td></tr>
                  <tr><td><code>--autopilot</code></td><td>Start in autopilot mode (equivalent to cycling to Autopilot immediately on launch)</td></tr>
                  <tr><td><code>--agent NAME</code></td><td>Start with a specific agent active — NAME is the filename without <code>.agent.md</code></td></tr>
                  <tr><td><code>--resume</code></td><td>Resume the most recent session with its full context intact</td></tr>
                  <tr><td><code>--allow-tool='TOOL(ARGS)'</code></td><td>Pre-approve one specific tool call pattern for the session</td></tr>
                  <tr><td><code>--deny-tool='TOOL'</code></td><td>Block a specific tool for the entire session</td></tr>
                </tbody>
              </table>
              <div class="code-block" data-lang="bash">
                <div class="code-block__header">
                  <span class="code-block__lang">bash</span>
                  <button class="code-block__copy" aria-label="Copy to clipboard">
                    <svg class="icon icon--copy" aria-hidden="true" width="14" height="14" viewBox="0 0 16 16" fill="currentColor">
                      <path d="M0 6.75C0 5.784.784 5 1.75 5h1.5a.75.75 0 010 1.5h-1.5a.25.25 0 00-.25.25v7.5c0 .138.112.25.25.25h7.5a.25.25 0 00.25-.25v-1.5a.75.75 0 011.5 0v1.5A1.75 1.75 0 019.25 16h-7.5A1.75 1.75 0 010 14.25v-7.5z"/><path d="M5 1.75C5 .784 5.784 0 6.75 0h7.5C15.216 0 16 .784 16 1.75v7.5A1.75 1.75 0 0114.25 11h-7.5A1.75 1.75 0 015 9.25v-7.5zm1.75-.25a.25.25 0 00-.25.25v7.5c0 .138.112.25.25.25h7.5a.25.25 0 00.25-.25v-7.5a.25.25 0 00-.25-.25h-7.5z"/>
                    </svg>
                    <svg class="icon icon--check icon--hidden" aria-hidden="true" width="14" height="14" viewBox="0 0 16 16" fill="currentColor">
                      <path d="M13.78 4.22a.75.75 0 010 1.06l-7.25 7.25a.75.75 0 01-1.06 0L2.22 9.28a.75.75 0 011.06-1.06L6 10.94l6.72-6.72a.75.75 0 011.06 0z"/>
                    </svg>
                    <span class="code-block__copy-label">Copy code</span>
                  </button>
                </div>
                <pre class="code-block__pre"><code class="language-bash"># Allow all tools — use with trusted, scoped tasks only
copilot --allow-all-tools -p "run the test suite and fix any failures"

# Pre-approve a specific tool call pattern for the session
copilot --allow-tool='shell(git log)' -p "summarize recent changes to main"

# Block shell access for the session
copilot --deny-tool='shell' -p "review the auth module for security issues"

# Resume the most recent session with context intact
copilot --resume</code></pre>
              </div>
            </div>
          </details>
        </section>
```

**Critical constraints to verify before writing:**
- Every `<code>` block starts with `copilot`, never `gh copilot suggest` (per RESEARCH.md Pitfall 1)
- Every `.code-block__copy` button contains both `icon--copy` and `icon--check icon--hidden` SVGs (per RESEARCH.md Pitfall 3)
- The section ID remains `cli-overview` — do NOT change it (sidebar scrollspy depends on it)
- No new `<section>` elements — all content fits within the existing `<section id="cli-overview">` (per RESEARCH.md Pitfall 4)
- No `<style>` additions — `style="margin: 24px 0 8px;"` on `<h3>` is inline and acceptable
  </action>

  <verify>
    <automated>
      powershell: Select-String -Path "index.html" -Pattern "copilot -p" -Quiet
      powershell: Select-String -Path "index.html" -Pattern "copilot -sp" -Quiet
      powershell: Select-String -Path "index.html" -Pattern "Shift\+Tab" -Quiet
      powershell: Select-String -Path "index.html" -Pattern "@FILENAME" -Quiet
      powershell: Select-String -Path "index.html" -Pattern "\-\-allow-all-tools" -Quiet
      powershell: Select-String -Path "index.html" -Pattern "\-\-deny-tool" -Quiet
      powershell: !(Select-String -Path "index.html" -Pattern "Content for this section will be added in Phase 2" -Quiet) -or (Select-String -Path "index.html" -Pattern "Content for this section will be added in Phase 2" | Where-Object { $_.LineNumber -lt 504 -or $_.LineNumber -gt 560 })
    </automated>
  </verify>

  <acceptance_criteria>
    - index.html contains the string `copilot -p`
    - index.html contains the string `copilot -sp`
    - index.html contains the string `copilot --agent`
    - index.html contains the string `Shift+Tab`
    - index.html contains the string `@FILENAME`
    - index.html contains the string `!COMMAND`
    - index.html contains the string `--allow-all-tools`
    - index.html contains the string `--deny-tool`
    - index.html contains the string `--resume`
    - index.html contains the string `Mode Cycling`
    - index.html contains the string `Power Flags`
    - index.html does NOT contain `gh copilot suggest` or `gh copilot explain`
    - The `cli-overview` section no longer contains "Content for this section will be added in Phase 2"
    - Every new `.code-block__copy` button contains both `icon--copy` and `icon--check icon--hidden` class SVGs
  </acceptance_criteria>

  <done>
    `#cli-overview` section contains: (1) narrative intro paragraphs, (2) command patterns bash code block with copilot/copilot -p/copilot -sp/copilot --agent, (3) Mode Cycling collapsible with Standard/Plan/Autopilot table, (4) Keyboard Shortcuts collapsible with @FILENAME/!COMMAND/Esc/Ctrl+L//help, (5) Power Flags collapsible with all 6 flags and example code block. No placeholder text remains.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace #cli-reference with slash commands reference and /research special-status callout</name>
  <files>index.html</files>

  <read_first>
    - index.html — read the `<section id="cli-reference">` block (lines ~557–579) to see what to replace
    - .planning/phases/02-cli-intro-agents-content/02-RESEARCH.md — §Slash Commands table and the /research note for verified content
  </read_first>

  <action>
Replace the ENTIRE `<section id="cli-reference" class="content-section">` block (from its opening `<section` tag through its closing `</section>`) with the following HTML. Use the copy-button and chevron SVG patterns from the interfaces context exactly.

```html
        <section id="cli-reference" class="content-section">
          <h2 class="section-heading">Slash Commands</h2>
          <p>Slash commands are typed at the session prompt during any interactive Copilot CLI session. They trigger specific behaviors — switching AI models, invoking agents, managing skills, orchestrating subagent workflows, or surfacing diffs and PRs. Most commands work in any mode; <code>/fleet</code> and <code>/delegate</code> are preview features with orchestrator requirements.</p>

          <details class="collapsible">
            <summary class="collapsible__trigger">
              <svg class="collapsible__chevron" aria-hidden="true" width="16" height="16" viewBox="0 0 16 16" fill="currentColor">
                <path d="M6.22 3.22a.75.75 0 011.06 0l4.25 4.25a.75.75 0 010 1.06l-4.25 4.25a.75.75 0 01-1.06-1.06L9.94 8 6.22 4.28a.75.75 0 010-1.06z"/>
              </svg>
              Slash Command Reference
            </summary>
            <div class="collapsible__body">
              <table>
                <thead>
                  <tr><th>Command</th><th>Purpose</th><th>Notes</th></tr>
                </thead>
                <tbody>
                  <tr><td><code>/plan</code></td><td>Draft an implementation plan before executing</td><td>Switches session to plan mode; you approve before execution begins</td></tr>
                  <tr><td><code>/fleet</code></td><td>Decompose the current task and run subagents in parallel</td><td>Preview feature — verify current interface with <code>/help</code></td></tr>
                  <tr><td><code>/model</code></td><td>Switch the active AI model mid-session</td><td>e.g. <code>/model claude-3-5-sonnet</code></td></tr>
                  <tr><td><code>/review</code></td><td>Invoke the <code>code-review</code> agent on the current context</td><td>Read-only; surfaces bugs and security issues only; never style feedback</td></tr>
                  <tr><td><code>/delegate</code></td><td>Hand off a subtask to a subagent explicitly</td><td>Preview feature — verify current interface with <code>/help</code></td></tr>
                  <tr><td><code>/research</code></td><td>Invoke the <code>research</code> agent for web fetch and search</td><td><strong>The only way to invoke the research built-in</strong> — never auto-triggered</td></tr>
                  <tr><td><code>/diff</code></td><td>Show the current working tree diff within the session</td><td></td></tr>
                  <tr><td><code>/pr</code></td><td>Create a pull request from the current working changes</td><td></td></tr>
                  <tr><td><code>/skills</code></td><td>Manage and list agent skills</td><td><code>/skills list</code>, <code>/skills info NAME</code>, <code>/skills add PATH</code>, <code>/skills reload</code>, <code>/skills remove DIR</code></td></tr>
                  <tr><td><code>/agent</code></td><td>Switch to or invoke a specific agent by name</td><td>e.g. <code>/agent gsd-executor</code> — switches the active agent mid-session</td></tr>
                </tbody>
              </table>
            </div>
          </details>

          <div class="callout callout--info">
            <span class="callout__type">Info</span>
            <p><strong><code>/research</code> is special.</strong> The built-in <code>research</code> agent — which performs web fetch and search — can <em>only</em> be invoked explicitly via <code>/research</code>. Unlike every other built-in agent, it is never auto-triggered by trigger-word inference, no matter how clearly your prompt implies a research task. If you need live web data in your session, you must type <code>/research</code>.</p>
          </div>

          <div class="callout callout--warning">
            <span class="callout__type">Warning</span>
            <p><strong><code>/fleet</code> and <code>/delegate</code> are preview features.</strong> They are functional in current releases but their syntax and parallel execution behavior may change. Use <code>/help</code> in your active session to verify the current interface before building workflows that depend on them.</p>
          </div>
        </section>
```

**Critical constraints:**
- Section ID must remain `cli-reference` — do NOT change it
- No new `<section>` elements — all content inside the existing `<section id="cli-reference">`
- The `/research` callout must use `callout--info` (not warning or danger) — it's informational, not alarming
- The `/fleet`/`/delegate` preview callout uses `callout--warning`
  </action>

  <verify>
    <automated>
      powershell: Select-String -Path "index.html" -Pattern "/research" -Quiet
      powershell: Select-String -Path "index.html" -Pattern "never auto-triggered" -Quiet
      powershell: Select-String -Path "index.html" -Pattern "/fleet" -Quiet
      powershell: Select-String -Path "index.html" -Pattern "/model" -Quiet
      powershell: Select-String -Path "index.html" -Pattern "Slash Command Reference" -Quiet
    </automated>
  </verify>

  <acceptance_criteria>
    - index.html contains the string `/research`
    - index.html contains the string `never auto-triggered`
    - index.html contains the string `/fleet`
    - index.html contains the string `/delegate`
    - index.html contains the string `/model`
    - index.html contains the string `/review`
    - index.html contains the string `/plan`
    - index.html contains the string `/diff`
    - index.html contains the string `/pr`
    - index.html contains the string `/skills`
    - index.html contains the string `/agent`
    - index.html contains `Slash Command Reference` (collapsible title)
    - The `cli-reference` section contains a `callout--info` block about `/research`
    - The `cli-reference` section contains a `callout--warning` block about preview features
    - The `cli-reference` section no longer contains "Content for this section will be added in Phase 2"
  </acceptance_criteria>

  <done>
    `#cli-reference` section contains: (1) narrative intro paragraph about slash commands, (2) collapsible "Slash Command Reference" table with all 10 commands (/plan, /fleet, /model, /review, /delegate, /research, /diff, /pr, /skills, /agent), (3) `callout--info` explaining /research's never-auto-triggered special status, (4) `callout--warning` for /fleet and /delegate preview status. No placeholder text remains.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, verify the full CLI tab is populated:

```powershell
# Check all CLI-01 through CLI-07 markers are present
Select-String -Path "index.html" -Pattern "copilot -p|copilot -sp|Shift\+Tab|@FILENAME|--allow-all-tools|/research|never auto-triggered" | Measure-Object | Select-Object -ExpandProperty Count
# Expect: 7 or more matches

# Verify no placeholder text remains in cli sections
Select-String -Path "index.html" -Pattern "Content for this section will be added in Phase 2"
# Expect: only matches in Skills tab sections (not CLI sections)

# Check file size is still under 200KB
(Get-Item "index.html").Length
# Expect: under 204800 bytes
```
</verification>

<success_criteria>
1. `#cli-overview` and `#cli-reference` sections have no placeholder text
2. All 7 CLI requirements have content: narrative intro (CLI-01), 4 command patterns (CLI-02), mode cycling collapsible (CLI-03), keyboard shortcuts table (CLI-04), 10-command slash reference (CLI-05), power flags table (CLI-06), /research callout (CLI-07)
3. All code blocks use `copilot` command (not `gh copilot suggest/explain`)
4. Every `.code-block__copy` button has the dual-SVG pattern
5. `index.html` is under 204,800 bytes
</success_criteria>

<output>
After completion, create `.planning/phases/02-cli-intro-agents-content/02-01-SUMMARY.md` with:
- What was implemented (sections replaced, requirements satisfied)
- Actual HTML patterns used (confirm no deviations from spec)
- File size after changes
- Any content decisions made (e.g., exact wording choices)
</output>
