---
phase: 02-cli-intro-agents-content
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified: []
autonomous: false
requirements: [CLI-01, CLI-02, CLI-03, CLI-04, CLI-05, CLI-06, CLI-07, AGNT-01, AGNT-02, AGNT-03, AGNT-04, AGNT-05, AGNT-06, AGNT-07, AGNT-08, AGNT-09, AGNT-10]

must_haves:
  truths:
    - "CLI Intro tab is readable and shows accurate copilot command content with no placeholder text"
    - "Agents tab opens with disambiguation callout before any narrative content"
    - "Code blocks have copy-to-clipboard buttons that work and show Copied! on success"
    - "Collapsibles open and close with chevron rotation"
    - "File size is under 200KB"
  artifacts:
    - path: "index.html"
      provides: "Complete Phase 2 content in CLI and Agents tabs"
      contains: "gsd-executor"
  key_links:
    - from: "index.html CLI Intro tab"
      to: "all 7 CLI requirements satisfied"
      via: "visual inspection"
      pattern: "Shift\\+Tab"
    - from: "index.html Agents tab"
      to: "all 10 AGNT requirements satisfied"
      via: "visual inspection"
      pattern: "\\.agent\\.md"
---

<objective>
Human verification that Phase 2 content is accurate, readable, and fully functional in both the CLI Intro and Agents tabs before marking the phase complete.

Purpose: Phase 2 is pure content — there are no automated tests for prose accuracy, visual correctness, or interactive component behavior. This checkpoint confirms the guide is actually usable before the phase is closed.

Output: Human approval or specific issues to fix before phase close.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-cli-intro-agents-content/02-01-SUMMARY.md
@.planning/phases/02-cli-intro-agents-content/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated pre-flight checks before human review</name>
  <files></files>

  <read_first>
    - index.html — quick scan to confirm no obvious regressions before opening in browser
  </read_first>

  <action>
Run these automated checks in sequence. Report any FAILs before proceeding to the human verification checkpoint:

```powershell
# 1. File size check (must be under 204,800 bytes)
$size = (Get-Item "index.html").Length
Write-Host "File size: $size bytes (limit: 204800) — $(if ($size -lt 204800) { 'PASS' } else { 'FAIL - OVER BUDGET' })"

# 2. No placeholder text in CLI or Agents sections (Skills placeholders are expected)
$placeholders = Select-String -Path "index.html" -Pattern "Content for this section will be added in Phase 2"
if ($placeholders) {
  $placeholders | ForEach-Object { Write-Host "PLACEHOLDER FOUND at line $($_.LineNumber): $($_.Line.Trim())" }
} else {
  Write-Host "Placeholder check: PASS — no placeholder text found"
}

# 3. Critical content presence checks
$checks = [ordered]@{
  "CLI-01 narrative"          = "standalone terminal AI agent"
  "CLI-02 command patterns"   = "copilot -p"
  "CLI-02 short alias"        = "copilot -sp"
  "CLI-03 mode cycling"       = "Shift\+Tab"
  "CLI-04 @FILENAME"          = "@FILENAME"
  "CLI-04 !COMMAND"           = "!COMMAND"
  "CLI-05 slash commands"     = "Slash Command Reference"
  "CLI-06 power flags"        = "--allow-all-tools"
  "CLI-07 /research special"  = "never auto-triggered"
  "AGNT-10 disambiguation"    = "Two agent systems share"
  "AGNT-02 .agent.md format"  = "\.agent\.md"
  "AGNT-03 gsd example"       = "gsd-executor"
  "AGNT-04 user location"     = "~/\.copilot/agents"
  "AGNT-05 built-in agents"   = "code-review"
  "AGNT-06 invocation"        = "Trigger-word inference"
  "AGNT-07 orchestration"     = "isolated context window"
  "AGNT-08 fleet"             = "Fleet Execution"
  "AGNT-09 naming conflicts"  = "user-level agents override"
}

$allPass = $true
foreach ($check in $checks.GetEnumerator()) {
  $found = Select-String -Path "index.html" -Pattern $check.Value -Quiet
  $status = if ($found) { "PASS" } else { "FAIL" }
  if (-not $found) { $allPass = $false }
  Write-Host "$($check.Key): $status"
}

if ($allPass) {
  Write-Host "`nAll automated checks PASSED. Proceeding to human verification."
} else {
  Write-Host "`nSome checks FAILED. Fix issues in the relevant plan before human verification."
}

# 4. Pitfall checks — verify no legacy commands slipped in
$legacy = Select-String -Path "index.html" -Pattern "gh copilot suggest|gh copilot explain"
if ($legacy) {
  Write-Host "PITFALL 1 FAIL: Legacy commands found — $($legacy.Line)"
} else {
  Write-Host "Pitfall 1 (legacy commands): PASS"
}

$wrongExt = Select-String -Path "index.html" -Pattern '\.github/agents/[^"]+\.md"' | 
  Where-Object { $_.Line -notmatch "\.agent\.md" -and $_.Line -notmatch "cloud agent" }
if ($wrongExt) {
  Write-Host "PITFALL 2 FAIL: Agent file without .agent.md extension — $($wrongExt.Line)"
} else {
  Write-Host "Pitfall 2 (.agent.md extension): PASS"
}
```
  </action>

  <verify>
    <automated>
      powershell: (Get-Item "index.html").Length -lt 204800
      powershell: !(Select-String -Path "index.html" -Pattern "gh copilot suggest|gh copilot explain" -Quiet)
      powershell: Select-String -Path "index.html" -Pattern "isolated context window" -Quiet
      powershell: Select-String -Path "index.html" -Pattern "gsd-executor" -Quiet
    </automated>
  </verify>

  <acceptance_criteria>
    - index.html is under 204,800 bytes
    - No `gh copilot suggest` or `gh copilot explain` in index.html
    - All 18 content checks (CLI-01 through AGNT-10) show PASS
    - No placeholder text in CLI or Agents tab sections
  </acceptance_criteria>

  <done>All automated pre-flight checks pass. Ready for human review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 2 content in `index.html`:

**CLI Intro tab (`#cli-overview` + `#cli-reference`):**
- CLI Overview section: narrative intro, command patterns code block (copilot / copilot -p / copilot -sp / copilot --agent), Mode Cycling collapsible (Standard/Plan/Autopilot table + Shift+Tab), Keyboard Shortcuts collapsible (@FILENAME/!COMMAND/Esc/Ctrl+L//help table), Power Flags collapsible (6-flag table + examples code block)
- Slash Commands section: narrative, Slash Command Reference collapsible (10-command table), /research info callout, /fleet+/delegate warning callout

**Agents tab (`#agents-overview` + `#agents-reference`):**
- Agents Overview section: disambiguation callout (CLI .agent.md vs cloud .md), what-agents-are narrative, Agent Profile Format h3, annotated gsd-executor.agent.md code block with filepath header, Frontmatter Field Reference collapsible, Agent File Locations collapsible (repo/user/org table), Naming Conflict Resolution collapsible
- Agents Reference section: Built-in Agents table (5 agents), Invocation Methods collapsible (4-row table + code block), Subagent Orchestration h3 + narrative, Fleet Execution collapsible (/fleet explanation + code block + preview warning callout)
  </what-built>

  <how-to-verify>
Open `index.html` directly in a browser (double-click or `start index.html`). Then verify each item below:

**CLI Intro tab — Overview section:**
1. Click "CLI Intro" tab → "CLI Intro — Overview" section is visible
2. Read the two introductory paragraphs — confirm "standalone terminal AI agent" and no mention of `gh copilot suggest`
3. Confirm the bash code block shows all 4 patterns: `copilot`, `copilot -p`, `copilot -sp`, `copilot --agent`
4. Click "Mode Cycling" collapsible → confirm table shows Standard / Plan / Autopilot rows with Shift+Tab mentioned
5. Click "Keyboard Shortcuts & Context Operators" collapsible → confirm @FILENAME, !COMMAND, Esc, Ctrl+L, /help in table
6. Click "Power Flags" collapsible → confirm 6-row table (--allow-all-tools, --autopilot, --agent, --resume, --allow-tool, --deny-tool) + example code block

**CLI Intro tab — Slash Commands section:**
7. Scroll to "Slash Commands" section
8. Click "Slash Command Reference" collapsible → confirm 10-command table includes /plan, /fleet, /model, /review, /delegate, /research, /diff, /pr, /skills, /agent
9. Verify the blue `callout--info` box says `/research is special` and explains it's never auto-triggered
10. Verify the yellow `callout--warning` box mentions /fleet and /delegate as preview features

**Agents tab — Overview section:**
11. Click "Agents" tab → confirm the tab switches correctly
12. Confirm the FIRST element after the "Agents Overview" heading is a blue `callout--info` with "Two agent systems share the same directory"
13. Read the two narrative paragraphs — confirm "isolated context window" and no mention of plain `.md` agents
14. Scroll to "Agent Profile Format" h3 → confirm the code block shows `.github/agents/gsd-executor.agent.md` as filepath label (not a lang badge)
15. Confirm the YAML code block has inline comments annotating each frontmatter field (name, description, tools, mcp-servers)
16. Click "Frontmatter Field Reference" collapsible → confirm 4-row table (name, description, tools, mcp-servers)
17. Click "Agent File Locations" collapsible → confirm 3-row table (.github/agents/, ~/.copilot/agents/, .github-private/)
18. Click "Naming Conflict Resolution" collapsible → confirm user > repo > org precedence is explained

**Agents tab — Reference section:**
19. Scroll to "Agents Reference" section
20. Confirm "Built-in Agents" table has exactly 5 rows: explore, task, general-purpose, code-review, research
21. Confirm `research` row says "/research only" in the Invoked By column
22. Click "Invocation Methods" collapsible → confirm 4-row table (slash command, prompt mention, trigger-word inference, CLI flag) + code block
23. Scroll to "Subagent Orchestration" h3 → read 2 paragraphs, confirm "isolated context window" appears
24. Click "Fleet Execution — /fleet" collapsible → confirm /fleet explanation + code block + yellow preview warning callout

**Interactive component checks:**
25. Click any copy button — confirm "Copied!" success state appears (green text + checkmark) and reverts after 2 seconds
26. Check that collapsible chevron icons rotate 90° when open
27. Verify code blocks have syntax highlighting (bash and yaml blocks should be colorized, not plain text)
28. Click Skills tab — confirm Skills tab still works and shows placeholder content (Phase 3 untouched)

**Acceptance threshold:** Items 1–28 all pass. Any failure in a numbered item should be reported with the item number and exact issue.
  </how-to-verify>

  <resume-signal>
If all checks pass: type "approved"
If issues found: describe each issue with its item number from the checklist above (e.g., "Item 14 fails: code block shows lang badge instead of filepath header")
  </resume-signal>
</task>

</tasks>

<verification>
Phase 2 is complete when:
1. All automated pre-flight checks pass (Task 1)
2. Human has reviewed all 28 checklist items and typed "approved"
3. No items in the verification checklist are outstanding

If issues are reported during human verification, create a targeted fix using `/gsd-quick` for small corrections before re-running this checkpoint.
</verification>

<success_criteria>
1. Human has opened index.html in a browser and verified all content sections
2. All interactive components (copy buttons, collapsibles, tab switching) work
3. No placeholder text visible in CLI Intro or Agents tabs
4. Agent file examples use `.agent.md` extension throughout
5. All code examples use `copilot` command (not legacy `gh copilot suggest/explain`)
6. File is under 200KB
7. Skills tab is untouched (Phase 3 placeholder content still present)
</success_criteria>

<output>
After human approval, create `.planning/phases/02-cli-intro-agents-content/02-03-SUMMARY.md` with:
- Verification result: approved/issues found
- Final file size
- Any issues discovered and how they were resolved
- Phase 2 completion status

Then update STATE.md:
- `status: Phase complete — ready for verification`
- `stopped_at: Phase 2 human verification approved`
- `current_focus: Phase 03 — skills-section-polish`
</output>
