---
phase: 03-skills-section-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [index.html]
autonomous: true
requirements: [SKIL-01, SKIL-02, SKIL-03, SKIL-04, SKIL-05, SKIL-06, SKIL-07, SKIL-08, SKIL-09, SKIL-10, SKIL-11]

must_haves:
  truths:
    - "Skills tab opens with a directory tree showing .github/skills/my-skill/SKILL.md structure before any narrative text"
    - "A brief distinction explains skills vs agents: skills don't get their own context window"
    - "A callout--info notes skills as an open standard (Agent Skills spec) across CLI, cloud, VS Code"
    - "A complete annotated deploy SKILL.md example is shown with filepath header and inline YAML comments"
    - "Frontmatter Field Reference collapsible has 4-row table (name, description, license, allowed-tools)"
    - "Script-enabled skill collapsible shows db-migrate SKILL.md + migrate.sh + callout--danger for shell warning"
    - "Security warning is strong and direct — 'arbitrary command execution' language, not hedged"
    - "Skill File Locations collapsible has 2-row table (.github/skills/ and ~/.copilot/skills/)"
    - "Skills Command Reference collapsible has 6-row table including /skill-name invocation"
    - "Community resources callout--tip references github.com/anthropics/skills and github.com/github/awesome-copilot"
    - "Page header shows 'Last verified: YYYY-MM-DD' in muted text"
    - "No placeholder text remains in skills-overview or skills-reference sections"
  artifacts:
    - path: "index.html"
      provides: "skills-overview and skills-reference section content plus header date"
      contains: "SKILL.md"
  key_links:
    - from: "#skills-overview"
      to: "directory tree, distinction, open standard callout, annotated SKILL.md, field reference, script example, file locations"
      via: "code-block + callout--info + filepath header + collapsibles"
      pattern: "deploy"
    - from: "#skills-reference"
      to: "CLI commands table, community resources"
      via: "collapsible table + callout--tip"
      pattern: "/skills list"
    - from: "site-header"
      to: "last verified date"
      via: "inline span with muted color"
      pattern: "Last verified"
---

<objective>
Replace all placeholder content in the Skills tab's two sections (`#skills-overview` and `#skills-reference`) with complete, accurate HTML content covering all eleven Skills requirements. Add a "last verified" date to the page header.

Purpose: The Skills tab completes the guide's final content section — users need to understand what skills are, how to create SKILL.md files, what security risks `allowed-tools: shell` carries, and how to manage skills from the CLI.

Output: `index.html` with `#skills-overview` and `#skills-reference` sections fully populated (no placeholder text) and a "Last verified" date in the page header.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-skills-section-polish/03-CONTEXT.md
@.planning/phases/03-skills-section-polish/03-RESEARCH.md
@.planning/REQUIREMENTS.md
@index.html
@.planning/phases/02-cli-intro-agents-content/02-02-SUMMARY.md

<interfaces>
<!-- Reusable SVG patterns — use EXACTLY these in every instance. Copied from existing index.html. -->

COPY BUTTON (use in every .code-block):
```html
<button class="code-block__copy" aria-label="Copy to clipboard">
  <svg class="icon icon--copy" aria-hidden="true" width="14" height="14" viewBox="0 0 16 16" fill="currentColor">
    <path d="M0 6.75C0 5.784.784 5 1.75 5h1.5a.75.75 0 010 1.5h-1.5a.25.25 0 00-.25.25v7.5c0 .138.112.25.25.25h7.5a.25.25 0 00.25-.25v-1.5a.75.75 0 011.5 0v1.5A1.75 1.75 0 019.25 16h-7.5A1.75 1.75 0 010 14.25v-7.5z"/><path d="M5 1.75C5 .784 5.784 0 6.75 0h7.5C15.216 0 16 .784 16 1.75v7.5A1.75 1.75 0 0114.25 11h-7.5A1.75 1.75 0 015 9.25v-7.5zm1.75-.25a.25.25 0 00-.25.25v7.5c0 .138.112.25.25.25h7.5a.25.25 0 00.25-.25v-7.5a.25.25 0 00-.25-.25h-7.5z"/>
  </svg>
  <svg class="icon icon--check icon--hidden" aria-hidden="true" width="14" height="14" viewBox="0 0 16 16" fill="currentColor">
    <path d="M13.78 4.22a.75.75 0 010 1.06l-7.25 7.25a.75.75 0 01-1.06 0L2.22 9.28a.75.75 0 011.06-1.06L6 10.94l6.72-6.72a.75.75 0 011.06 0z"/>
  </svg>
  <span class="code-block__copy-label">Copy code</span>
</button>
```

COLLAPSIBLE CHEVRON SVG (use in every <summary>):
```html
<svg class="collapsible__chevron" aria-hidden="true" width="16" height="16" viewBox="0 0 16 16" fill="currentColor">
  <path d="M6.22 3.22a.75.75 0 011.06 0l4.25 4.25a.75.75 0 010 1.06l-4.25 4.25a.75.75 0 01-1.06-1.06L9.94 8 6.22 4.28a.75.75 0 010-1.06z"/>
</svg>
```

FILE ICON SVG (12×12, use in .code-block__filepath):
```html
<svg aria-hidden="true" width="12" height="12" viewBox="0 0 16 16" fill="currentColor">
  <path d="M2 1.75C2 .784 2.784 0 3.75 0h6.586c.464 0 .909.184 1.237.513l2.914 2.914c.329.328.513.773.513 1.237v9.586A1.75 1.75 0 0113.25 16h-9.5A1.75 1.75 0 012 14.25V1.75z"/>
</svg>
```

FILEPATH CODE BLOCK HEADER (use for SKILL.md and .sh file examples):
```html
<div class="code-block__header">
  <span class="code-block__filepath">
    <svg aria-hidden="true" width="12" height="12" viewBox="0 0 16 16" fill="currentColor">
      <path d="M2 1.75C2 .784 2.784 0 3.75 0h6.586c.464 0 .909.184 1.237.513l2.914 2.914c.329.328.513.773.513 1.237v9.586A1.75 1.75 0 0113.25 16h-9.5A1.75 1.75 0 012 14.25V1.75z"/>
    </svg>
    {filepath goes here}
  </span>
  {COPY BUTTON goes here}
</div>
```

STANDARD CODE BLOCK HEADER (use for directory tree / generic bash):
```html
<div class="code-block__header">
  <span class="code-block__lang">bash</span>
  {COPY BUTTON goes here}
</div>
```

SECTIONS BEING REPLACED:
- `<section id="skills-overview">` — lines 1022–1073 (placeholder content: sample code block + sample collapsible + 4 sample callouts)
- `<section id="skills-reference">` — lines 1075–1095 (placeholder content: sample code block)
- Page header subtitle: line 436 (`<span class="site-header__subtitle">Agents &amp; Skills Guide</span>`)

HEADING PATTERN:
- Agents tab uses "Agents Overview" and "Agents Reference" (no em-dash)
- Normalize Skills to match: "Skills Overview" and "Skills Reference" (drop the em-dash from placeholders)
- h3 right after h2: `style="margin: 0 0 8px;"`
- h3 after other content: `style="margin: 24px 0 8px;"`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace #skills-overview with directory tree, annotated SKILL.md, field reference, script example with security warning, and file locations</name>
  <files>index.html</files>

  <read_first>
    - index.html — read the entire `<section id="skills-overview">` block (lines 1022–1073) to see what placeholder content to replace
    - index.html — read lines 717–806 to see the Agents tab's code-block + collapsible patterns (filepath header, frontmatter field reference table) that must be mirrored
    - .planning/phases/03-skills-section-polish/03-CONTEXT.md — locked decisions D-01 through D-09, D-12, D-13
    - .planning/phases/03-skills-section-polish/03-RESEARCH.md — §Content Domain Knowledge (SKILL.md format, frontmatter fields, security warning text), §Code Examples (deploy skill, db-migrate skill, migrate.sh)
  </read_first>

  <action>
Replace the ENTIRE `<section id="skills-overview" class="content-section">` block (from its opening `<section` tag through its closing `</section>` at line 1073) with the following HTML. The heading changes from "Skills — Overview" to "Skills Overview" (no em-dash, matching Agents tab convention). Use the SVG patterns from the interfaces context exactly.

The directory tree (per D-01) MUST be the first element after the `<h2>` — before any narrative text.

```html
        <section id="skills-overview" class="content-section">
          <h2 class="section-heading">Skills Overview</h2>

          <div class="code-block" data-lang="bash">
            <div class="code-block__header">
              <span class="code-block__lang">bash</span>
              <button class="code-block__copy" aria-label="Copy to clipboard">
                <svg class="icon icon--copy" aria-hidden="true" width="14" height="14" viewBox="0 0 16 16" fill="currentColor">
                  <path d="M0 6.75C0 5.784.784 5 1.75 5h1.5a.75.75 0 010 1.5h-1.5a.25.25 0 00-.25.25v7.5c0 .138.112.25.25.25h7.5a.25.25 0 00.25-.25v-1.5a.75.75 0 011.5 0v1.5A1.75 1.75 0 019.25 16h-7.5A1.75 1.75 0 010 14.25v-7.5z"/><path d="M5 1.75C5 .784 5.784 0 6.75 0h7.5C15.216 0 16 .784 16 1.75v7.5A1.75 1.75 0 0114.25 11h-7.5A1.75 1.75 0 015 9.25v-7.5zm1.75-.25a.25.25 0 00-.25.25v7.5c0 .138.112.25.25.25h7.5a.25.25 0 00.25-.25v-7.5a.25.25 0 00-.25-.25h-7.5z"/>
                </svg>
                <svg class="icon icon--check icon--hidden" aria-hidden="true" width="14" height="14" viewBox="0 0 16 16" fill="currentColor">
                  <path d="M13.78 4.22a.75.75 0 010 1.06l-7.25 7.25a.75.75 0 01-1.06 0L2.22 9.28a.75.75 0 011.06-1.06L6 10.94l6.72-6.72a.75.75 0 011.06 0z"/>
                </svg>
                <span class="code-block__copy-label">Copy code</span>
              </button>
            </div>
            <pre class="code-block__pre"><code class="language-bash">.github/
└── skills/
    └── my-skill/
        ├── SKILL.md          # required — skill definition
        └── helpers/           # optional — companion scripts
            └── run.sh</code></pre>
          </div>

          <p>Skills are reusable instruction bundles — unlike agents, they don't get their own context window. When you invoke a skill with <code>/skill-name</code>, Copilot loads the skill's instructions into the current session alongside your conversation. Think of skills as modular expertise you can pull in on demand: a deploy checklist, a code review procedure, a database migration workflow. They're lighter than agents (no isolated context, no persona) but more structured than a raw prompt.</p>

          <div class="callout callout--info">
            <span class="callout__type">Info</span>
            <p><strong>Skills are an open standard.</strong> The Agent Skills specification defines a portable format that works across Copilot CLI, GitHub cloud agents, and VS Code agent mode. A <code>SKILL.md</code> file you write for the CLI works identically in all three surfaces — no adaptation needed.</p>
          </div>

          <h3 style="margin: 24px 0 8px;">SKILL.md Format</h3>
          <p>A skill is defined by a <code>SKILL.md</code> file inside a named subdirectory under <code>.github/skills/</code>. The directory name becomes the skill's invocation name. YAML frontmatter declares the skill's identity and permissions; the Markdown body becomes the instructions Copilot follows when the skill is active.</p>

          <div class="code-block" data-lang="yaml">
            <div class="code-block__header">
              <span class="code-block__filepath">
                <svg aria-hidden="true" width="12" height="12" viewBox="0 0 16 16" fill="currentColor">
                  <path d="M2 1.75C2 .784 2.784 0 3.75 0h6.586c.464 0 .909.184 1.237.513l2.914 2.914c.329.328.513.773.513 1.237v9.586A1.75 1.75 0 0113.25 16h-9.5A1.75 1.75 0 012 14.25V1.75z"/>
                </svg>
                .github/skills/deploy/SKILL.md
              </span>
              <button class="code-block__copy" aria-label="Copy to clipboard">
                <svg class="icon icon--copy" aria-hidden="true" width="14" height="14" viewBox="0 0 16 16" fill="currentColor">
                  <path d="M0 6.75C0 5.784.784 5 1.75 5h1.5a.75.75 0 010 1.5h-1.5a.25.25 0 00-.25.25v7.5c0 .138.112.25.25.25h7.5a.25.25 0 00.25-.25v-1.5a.75.75 0 011.5 0v1.5A1.75 1.75 0 019.25 16h-7.5A1.75 1.75 0 010 14.25v-7.5z"/><path d="M5 1.75C5 .784 5.784 0 6.75 0h7.5C15.216 0 16 .784 16 1.75v7.5A1.75 1.75 0 0114.25 11h-7.5A1.75 1.75 0 015 9.25v-7.5zm1.75-.25a.25.25 0 00-.25.25v7.5c0 .138.112.25.25.25h7.5a.25.25 0 00.25-.25v-7.5a.25.25 0 00-.25-.25h-7.5z"/>
                </svg>
                <svg class="icon icon--check icon--hidden" aria-hidden="true" width="14" height="14" viewBox="0 0 16 16" fill="currentColor">
                  <path d="M13.78 4.22a.75.75 0 010 1.06l-7.25 7.25a.75.75 0 01-1.06 0L2.22 9.28a.75.75 0 011.06-1.06L6 10.94l6.72-6.72a.75.75 0 011.06 0z"/>
                </svg>
                <span class="code-block__copy-label">Copy code</span>
              </button>
            </div>
            <pre class="code-block__pre"><code class="language-yaml">---
name: deploy                              # required — lowercase-hyphens; invoked as /deploy
description: >                            # required — Copilot reads this to decide when to suggest
  Deploys the current project to the configured environment. Reads deployment
  config from deploy.yaml, runs pre-deploy checks, and executes the deploy
  pipeline. Use when asked to deploy, release, or push to production.
license: MIT                              # optional — metadata only; not enforced by Copilot
allowed-tools:                            # optional — restricts which tools this skill may use
  - shell                                 #   ⚠ grants arbitrary command execution
  - view                                  #   read files and directories
---

# Deploy Skill

When invoked, follow this sequence:

1. Read `deploy.yaml` from the project root
2. Run pre-deploy checks: `npm test` and `npm run lint`
3. If checks pass, execute the deploy command from the config
4. Report the deployment status with the environment URL</code></pre>
          </div>

          <details class="collapsible">
            <summary class="collapsible__trigger">
              <svg class="collapsible__chevron" aria-hidden="true" width="16" height="16" viewBox="0 0 16 16" fill="currentColor">
                <path d="M6.22 3.22a.75.75 0 011.06 0l4.25 4.25a.75.75 0 010 1.06l-4.25 4.25a.75.75 0 01-1.06-1.06L9.94 8 6.22 4.28a.75.75 0 010-1.06z"/>
              </svg>
              Frontmatter Field Reference
            </summary>
            <div class="collapsible__body">
              <table>
                <thead>
                  <tr><th>Field</th><th>Required?</th><th>Type</th><th>Purpose</th></tr>
                </thead>
                <tbody>
                  <tr>
                    <td><code>name</code></td>
                    <td>Yes</td>
                    <td>string (lowercase-hyphens)</td>
                    <td>Skill identifier — this is what you type after <code>/</code> to invoke the skill (e.g., <code>/deploy</code>). Must match the directory name.</td>
                  </tr>
                  <tr>
                    <td><code>description</code></td>
                    <td>Yes</td>
                    <td>string</td>
                    <td>Tells Copilot <em>what</em> this skill does. Used for suggestion matching — when a prompt aligns with the description, Copilot may recommend the skill. Be specific about the task domain.</td>
                  </tr>
                  <tr>
                    <td><code>license</code></td>
                    <td>No</td>
                    <td>string</td>
                    <td>License identifier (e.g., <code>"MIT"</code>). Metadata only — not enforced by Copilot. Useful for shared or community skills.</td>
                  </tr>
                  <tr>
                    <td><code>allowed-tools</code></td>
                    <td>No</td>
                    <td>list of strings</td>
                    <td>Tools this skill may use. Valid values: <code>shell</code>, <code>view</code>, <code>write</code>, <code>search</code>. If omitted, the skill inherits the session's tool permissions. <strong><code>shell</code> grants arbitrary command execution — see security warning below.</strong></td>
                  </tr>
                </tbody>
              </table>
            </div>
          </details>

          <details class="collapsible">
            <summary class="collapsible__trigger">
              <svg class="collapsible__chevron" aria-hidden="true" width="16" height="16" viewBox="0 0 16 16" fill="currentColor">
                <path d="M6.22 3.22a.75.75 0 011.06 0l4.25 4.25a.75.75 0 010 1.06l-4.25 4.25a.75.75 0 01-1.06-1.06L9.94 8 6.22 4.28a.75.75 0 010-1.06z"/>
              </svg>
              Script-Enabled Skill Example
            </summary>
            <div class="collapsible__body">
              <p>A skill can include companion scripts that Copilot executes via <code>allowed-tools: shell</code>. The SKILL.md body references the script by relative path; Copilot runs it when the skill is invoked.</p>

              <div class="code-block" data-lang="yaml">
                <div class="code-block__header">
                  <span class="code-block__filepath">
                    <svg aria-hidden="true" width="12" height="12" viewBox="0 0 16 16" fill="currentColor">
                      <path d="M2 1.75C2 .784 2.784 0 3.75 0h6.586c.464 0 .909.184 1.237.513l2.914 2.914c.329.328.513.773.513 1.237v9.586A1.75 1.75 0 0113.25 16h-9.5A1.75 1.75 0 012 14.25V1.75z"/>
                    </svg>
                    .github/skills/db-migrate/SKILL.md
                  </span>
                  <button class="code-block__copy" aria-label="Copy to clipboard">
                    <svg class="icon icon--copy" aria-hidden="true" width="14" height="14" viewBox="0 0 16 16" fill="currentColor">
                      <path d="M0 6.75C0 5.784.784 5 1.75 5h1.5a.75.75 0 010 1.5h-1.5a.25.25 0 00-.25.25v7.5c0 .138.112.25.25.25h7.5a.25.25 0 00.25-.25v-1.5a.75.75 0 011.5 0v1.5A1.75 1.75 0 019.25 16h-7.5A1.75 1.75 0 010 14.25v-7.5z"/><path d="M5 1.75C5 .784 5.784 0 6.75 0h7.5C15.216 0 16 .784 16 1.75v7.5A1.75 1.75 0 0114.25 11h-7.5A1.75 1.75 0 015 9.25v-7.5zm1.75-.25a.25.25 0 00-.25.25v7.5c0 .138.112.25.25.25h7.5a.25.25 0 00.25-.25v-7.5a.25.25 0 00-.25-.25h-7.5z"/>
                    </svg>
                    <svg class="icon icon--check icon--hidden" aria-hidden="true" width="14" height="14" viewBox="0 0 16 16" fill="currentColor">
                      <path d="M13.78 4.22a.75.75 0 010 1.06l-7.25 7.25a.75.75 0 01-1.06 0L2.22 9.28a.75.75 0 011.06-1.06L6 10.94l6.72-6.72a.75.75 0 011.06 0z"/>
                    </svg>
                    <span class="code-block__copy-label">Copy code</span>
                  </button>
                </div>
                <pre class="code-block__pre"><code class="language-yaml">---
name: db-migrate
description: >
  Runs database migrations using the companion shell script. Use when asked
  to migrate, update schema, or apply pending database changes.
allowed-tools:
  - shell
---

# Database Migration Skill

Run the companion migration script located at `./helpers/migrate.sh`.
Pass the target environment as the first argument.</code></pre>
              </div>

              <div class="code-block" data-lang="bash">
                <div class="code-block__header">
                  <span class="code-block__filepath">
                    <svg aria-hidden="true" width="12" height="12" viewBox="0 0 16 16" fill="currentColor">
                      <path d="M2 1.75C2 .784 2.784 0 3.75 0h6.586c.464 0 .909.184 1.237.513l2.914 2.914c.329.328.513.773.513 1.237v9.586A1.75 1.75 0 0113.25 16h-9.5A1.75 1.75 0 012 14.25V1.75z"/>
                    </svg>
                    .github/skills/db-migrate/helpers/migrate.sh
                  </span>
                  <button class="code-block__copy" aria-label="Copy to clipboard">
                    <svg class="icon icon--copy" aria-hidden="true" width="14" height="14" viewBox="0 0 16 16" fill="currentColor">
                      <path d="M0 6.75C0 5.784.784 5 1.75 5h1.5a.75.75 0 010 1.5h-1.5a.25.25 0 00-.25.25v7.5c0 .138.112.25.25.25h7.5a.25.25 0 00.25-.25v-1.5a.75.75 0 011.5 0v1.5A1.75 1.75 0 019.25 16h-7.5A1.75 1.75 0 010 14.25v-7.5z"/><path d="M5 1.75C5 .784 5.784 0 6.75 0h7.5C15.216 0 16 .784 16 1.75v7.5A1.75 1.75 0 0114.25 11h-7.5A1.75 1.75 0 015 9.25v-7.5zm1.75-.25a.25.25 0 00-.25.25v7.5c0 .138.112.25.25.25h7.5a.25.25 0 00.25-.25v-7.5a.25.25 0 00-.25-.25h-7.5z"/>
                    </svg>
                    <svg class="icon icon--check icon--hidden" aria-hidden="true" width="14" height="14" viewBox="0 0 16 16" fill="currentColor">
                      <path d="M13.78 4.22a.75.75 0 010 1.06l-7.25 7.25a.75.75 0 01-1.06 0L2.22 9.28a.75.75 0 011.06-1.06L6 10.94l6.72-6.72a.75.75 0 011.06 0z"/>
                    </svg>
                    <span class="code-block__copy-label">Copy code</span>
                  </button>
                </div>
                <pre class="code-block__pre"><code class="language-bash">#!/usr/bin/env bash
set -euo pipefail

ENV="${1:-development}"
echo "Running migrations for: $ENV"
npx prisma migrate deploy --schema=./prisma/schema.prisma
echo "Migrations complete."</code></pre>
              </div>

              <div class="callout callout--danger">
                <span class="callout__type">Danger</span>
                <p><strong><code>allowed-tools: shell</code> grants arbitrary command execution.</strong> A skill with shell access can run any command on your machine with your user permissions — including modifying files, installing packages, accessing credentials, and making network requests. Only enable shell access for skills you have personally reviewed and trust. Never grant shell access to third-party skills without reading every line of their source.</p>
              </div>
            </div>
          </details>

          <details class="collapsible">
            <summary class="collapsible__trigger">
              <svg class="collapsible__chevron" aria-hidden="true" width="16" height="16" viewBox="0 0 16 16" fill="currentColor">
                <path d="M6.22 3.22a.75.75 0 011.06 0l4.25 4.25a.75.75 0 010 1.06l-4.25 4.25a.75.75 0 01-1.06-1.06L9.94 8 6.22 4.28a.75.75 0 010-1.06z"/>
              </svg>
              Skill File Locations
            </summary>
            <div class="collapsible__body">
              <table>
                <thead>
                  <tr><th>Scope</th><th>Path</th><th>Available To</th></tr>
                </thead>
                <tbody>
                  <tr>
                    <td>Project (repo)</td>
                    <td><code>.github/skills/</code></td>
                    <td>Anyone with repo access</td>
                  </tr>
                  <tr>
                    <td>Personal (user)</td>
                    <td><code>~/.copilot/skills/</code></td>
                    <td>Only you, across all repos</td>
                  </tr>
                </tbody>
              </table>
            </div>
          </details>
        </section>
```

**Critical checks after replacement:**
- `id="skills-overview"` MUST remain unchanged (sidebar nav link at line 488 targets it)
- Heading text is now "Skills Overview" (no em-dash)
- Directory tree is FIRST element after `<h2>` (per D-01)
- Deploy SKILL.md code block uses `data-lang="yaml"` and `class="language-yaml"` (not bash)
- Shell script code block uses `data-lang="bash"` and `class="language-bash"`
- Security warning says "arbitrary command execution" (not softened per D-13)
- No leftover placeholder text ("Content for this section will be added in Phase 2")
  </action>

  <verify>
    <automated>
powershell: (Select-String -Path "index.html" -Pattern "Skills Overview" -Quiet) -and !(Select-String -Path "index.html" -Pattern "Skills — Overview" -Quiet)
powershell: Select-String -Path "index.html" -Pattern "skills/my-skill/" -Quiet
powershell: Select-String -Path "index.html" -Pattern "skills/deploy/SKILL.md" -Quiet
powershell: Select-String -Path "index.html" -Pattern "arbitrary command execution" -Quiet
powershell: Select-String -Path "index.html" -Pattern "Frontmatter Field Reference" -Quiet
powershell: Select-String -Path "index.html" -Pattern "db-migrate" -Quiet
powershell: Select-String -Path "index.html" -Pattern "~/.copilot/skills/" -Quiet
powershell: !(Select-String -Path "index.html" -Pattern "Content for this section will be added in Phase 2" -Quiet -CaseSensitive) -or ((Select-String -Path "index.html" -Pattern "Content for this section will be added in Phase 2" | Measure-Object).Count -eq 0)
    </automated>
  </verify>

  <acceptance_criteria>
    - index.html contains `Skills Overview` (no em-dash) as h2 heading text
    - index.html does NOT contain `Skills — Overview` (old placeholder heading)
    - index.html contains `skills/my-skill/` (directory tree present)
    - index.html contains `skills/deploy/SKILL.md` (annotated example filepath)
    - index.html contains `arbitrary command execution` (security warning is strong)
    - index.html contains `Frontmatter Field Reference` (collapsible title)
    - index.html contains `Script-Enabled Skill Example` (collapsible title)
    - index.html contains `db-migrate` (script-enabled example name)
    - index.html contains `migrate.sh` (companion shell script)
    - index.html contains `~/.copilot/skills/` (personal file location)
    - index.html contains `Skills are an open standard` (open standard callout)
    - index.html contains `Skill File Locations` (collapsible title)
    - index.html does NOT contain `Content for this section will be added in Phase 2` in the skills-overview section
    - All code blocks use `code-block__copy` button with both SVG icons (copy + check)
    - The deploy SKILL.md code block uses `code-block__filepath` header (not `code-block__lang`)
  </acceptance_criteria>

  <done>skills-overview section fully populated with directory tree (first), narrative distinction, open standard callout, annotated deploy SKILL.md with filepath header, Frontmatter Field Reference collapsible, Script-Enabled Skill Example collapsible (db-migrate + migrate.sh + danger callout), and Skill File Locations collapsible. No placeholder text remains.</done>
</task>

<task type="auto">
  <name>Task 2: Replace #skills-reference with CLI commands table and community resources, add "Last verified" date to page header</name>
  <files>index.html</files>

  <read_first>
    - index.html — read the entire `<section id="skills-reference">` block (lines 1075–1095) to see what placeholder content to replace
    - index.html — read lines 856–860 to see the Agents Reference heading pattern (h3 with margin: 0 0 8px right after h2)
    - index.html — read line 436 to see the current page header subtitle
    - .planning/phases/03-skills-section-polish/03-CONTEXT.md — locked decisions D-10, D-11, D-14
    - .planning/phases/03-skills-section-polish/03-RESEARCH.md — §CLI Commands table, §Community Resources, §"Last Verified" Date
  </read_first>

  <action>
**Part A: Replace skills-reference section**

Replace the ENTIRE `<section id="skills-reference" class="content-section">` block (from its opening `<section` tag through its closing `</section>`) with the following HTML. The heading changes from "Skills — Reference" to "Skills Reference" (no em-dash, matching Agents tab convention).

```html
        <section id="skills-reference" class="content-section">
          <h2 class="section-heading">Skills Reference</h2>

          <h3 style="margin: 0 0 8px;">Skills CLI Commands</h3>
          <p>Manage and invoke skills from the Copilot CLI. The <code>/skills</code> prefix handles discovery and management; invoking a skill directly uses <code>/skill-name</code> syntax with the skill's directory name.</p>

          <details class="collapsible">
            <summary class="collapsible__trigger">
              <svg class="collapsible__chevron" aria-hidden="true" width="16" height="16" viewBox="0 0 16 16" fill="currentColor">
                <path d="M6.22 3.22a.75.75 0 011.06 0l4.25 4.25a.75.75 0 010 1.06l-4.25 4.25a.75.75 0 01-1.06-1.06L9.94 8 6.22 4.28a.75.75 0 010-1.06z"/>
              </svg>
              Skills Command Reference
            </summary>
            <div class="collapsible__body">
              <table>
                <thead>
                  <tr><th>Command</th><th>Purpose</th><th>Notes</th></tr>
                </thead>
                <tbody>
                  <tr>
                    <td><code>/skills list</code></td>
                    <td>List all discovered skills</td>
                    <td>Shows project + personal skills; includes skill name and source path</td>
                  </tr>
                  <tr>
                    <td><code>/skills info SKILL</code></td>
                    <td>Show details about a specific skill</td>
                    <td>Displays name, description, allowed-tools, and file location</td>
                  </tr>
                  <tr>
                    <td><code>/skills add PATH</code></td>
                    <td>Add a skill from a filesystem path</td>
                    <td>The path must contain a <code>SKILL.md</code> file in the expected directory structure</td>
                  </tr>
                  <tr>
                    <td><code>/skills reload</code></td>
                    <td>Reload all skills</td>
                    <td>Pick up changes to SKILL.md files without restarting the session</td>
                  </tr>
                  <tr>
                    <td><code>/skills remove SKILL-DIRECTORY</code></td>
                    <td>Remove a previously added skill</td>
                    <td>Removes by directory name, not by skill name</td>
                  </tr>
                  <tr>
                    <td><code>/skill-name</code></td>
                    <td>Invoke a skill directly</td>
                    <td>Use the skill's directory name (e.g., <code>/deploy</code>, <code>/db-migrate</code>). Loads the skill's instructions into the current session.</td>
                  </tr>
                </tbody>
              </table>
            </div>
          </details>

          <div class="callout callout--tip">
            <span class="callout__type">Tip</span>
            <p><strong>Community skills are available.</strong> Check <code>github.com/anthropics/skills</code> for Anthropic's public skills repository and <code>github.com/github/awesome-copilot</code> for GitHub's curated list of Copilot resources. Skills follow an open standard — any SKILL.md you find in the community works with your local Copilot CLI installation.</p>
          </div>
        </section>
```

**Part B: Add "Last verified" date to page header**

Find this line (around line 436):
```html
      <span class="site-header__subtitle">Agents &amp; Skills Guide</span>
```

Replace it with (per D-14 — use the actual current date at execution time in YYYY-MM-DD format):
```html
      <span class="site-header__subtitle">Agents &amp; Skills Guide</span>
      <span style="color: var(--color-text-secondary); font-size: 13px; margin-left: 12px;">Last verified: YYYY-MM-DD</span>
```

Where `YYYY-MM-DD` is TODAY's date (the date you execute this task). Determine it by running `Get-Date -Format "yyyy-MM-dd"` in PowerShell.

**Critical checks after replacement:**
- `id="skills-reference"` MUST remain unchanged (sidebar nav link at line 489 targets it)
- Heading text is now "Skills Reference" (no em-dash)
- CLI commands table has exactly 6 rows: /skills list, /skills info, /skills add, /skills reload, /skills remove, /skill-name
- Community callout is a `callout--tip` (not info or warning)
- "Last verified" date uses `var(--color-text-secondary)` for the muted color
- No leftover placeholder text in skills-reference
  </action>

  <verify>
    <automated>
powershell: (Select-String -Path "index.html" -Pattern "Skills Reference" -Quiet) -and !(Select-String -Path "index.html" -Pattern "Skills — Reference" -Quiet)
powershell: Select-String -Path "index.html" -Pattern "/skills list" -Quiet
powershell: Select-String -Path "index.html" -Pattern "/skills remove SKILL-DIRECTORY" -Quiet
powershell: Select-String -Path "index.html" -Pattern "/skill-name" -Quiet
powershell: Select-String -Path "index.html" -Pattern "anthropics/skills" -Quiet
powershell: Select-String -Path "index.html" -Pattern "awesome-copilot" -Quiet
powershell: Select-String -Path "index.html" -Pattern "Last verified:" -Quiet
powershell: (Get-Item "index.html").Length -lt 204800
    </automated>
  </verify>

  <acceptance_criteria>
    - index.html contains `Skills Reference` (no em-dash) as h2 heading text
    - index.html does NOT contain `Skills — Reference` (old placeholder heading)
    - index.html contains `/skills list` in a table cell
    - index.html contains `/skills info SKILL` in a table cell
    - index.html contains `/skills add PATH` in a table cell
    - index.html contains `/skills reload` in a table cell
    - index.html contains `/skills remove SKILL-DIRECTORY` in a table cell
    - index.html contains `/skill-name` (invocation syntax row)
    - index.html contains `Skills Command Reference` (collapsible title)
    - index.html contains `anthropics/skills` (community resource)
    - index.html contains `awesome-copilot` (community resource)
    - index.html contains `callout--tip` wrapping the community resources
    - index.html contains `Last verified:` in the page header area
    - index.html contains `--color-text-secondary` in the last verified span
    - index.html file size is under 204,800 bytes
    - No placeholder text remains anywhere in the skills tab
  </acceptance_criteria>

  <done>skills-reference section fully populated with CLI commands collapsible table (6 rows including /skill-name invocation) and community resources callout--tip. Page header shows "Last verified: YYYY-MM-DD" in muted text. No placeholder text remains. File is under 200KB.</done>
</task>

</tasks>

<verification>
After both tasks complete, run this consolidated verification:

```powershell
# File size check
$size = (Get-Item "index.html").Length
Write-Host "File size: $size bytes (limit: 204800) — $(if ($size -lt 204800) { 'PASS' } else { 'FAIL' })"

# No placeholder text remaining in skills sections
$placeholders = Select-String -Path "index.html" -Pattern "Content for this section will be added"
if ($placeholders) {
  Write-Host "FAIL: Placeholder text found at lines: $($placeholders.LineNumber -join ', ')"
} else {
  Write-Host "Placeholder check: PASS"
}

# All SKIL requirements presence checks
$checks = [ordered]@{
  "SKIL-01 distinction"     = "don't get their own context window"
  "SKIL-02 open standard"   = "Skills are an open standard"
  "SKIL-03 directory tree"  = "skills/my-skill/"
  "SKIL-04 field reference" = "Frontmatter Field Reference"
  "SKIL-05 annotated example" = "skills/deploy/SKILL.md"
  "SKIL-06 script example"  = "db-migrate"
  "SKIL-07 security warning" = "arbitrary command execution"
  "SKIL-08 file locations"  = "~/.copilot/skills/"
  "SKIL-09 CLI commands"    = "/skills list"
  "SKIL-10 invocation"      = "/skill-name"
  "SKIL-11 community"       = "anthropics/skills"
}

$allPass = $true
foreach ($check in $checks.GetEnumerator()) {
  $found = Select-String -Path "index.html" -Pattern $check.Value -Quiet
  $status = if ($found) { "PASS" } else { "FAIL" }
  if (-not $found) { $allPass = $false }
  Write-Host "$($check.Key): $status"
}

# Header date check
$dateCheck = Select-String -Path "index.html" -Pattern "Last verified:" -Quiet
Write-Host "D-14 header date: $(if ($dateCheck) { 'PASS' } else { 'FAIL' })"

# Heading normalization check
$oldHeadings = Select-String -Path "index.html" -Pattern "Skills —"
if ($oldHeadings) {
  Write-Host "FAIL: Old em-dash headings still present at lines: $($oldHeadings.LineNumber -join ', ')"
} else {
  Write-Host "Heading normalization: PASS"
}

if ($allPass -and $dateCheck -and !$placeholders -and !$oldHeadings -and ($size -lt 204800)) {
  Write-Host "`nALL CHECKS PASSED"
} else {
  Write-Host "`nSOME CHECKS FAILED — review above"
}
```
</verification>

<success_criteria>
1. skills-overview section has: directory tree (first), narrative distinction, open standard callout, annotated SKILL.md with filepath header, Frontmatter Field Reference collapsible, Script-Enabled Skill Example collapsible with danger callout, Skill File Locations collapsible
2. skills-reference section has: CLI commands collapsible with 6-row table, community resources callout--tip
3. Page header shows "Last verified: YYYY-MM-DD" in muted text
4. No placeholder text remains anywhere in the Skills tab
5. Headings normalized to "Skills Overview" / "Skills Reference" (no em-dash)
6. File size under 200KB (204,800 bytes)
7. All 11 SKIL requirements have corresponding content verifiable by grep
</success_criteria>

<output>
After completion, create `.planning/phases/03-skills-section-polish/03-01-SUMMARY.md`
</output>
